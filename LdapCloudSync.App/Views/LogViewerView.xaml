<UserControl x:Class="LdapCloudSync.App.Views.LogViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: log file list -->
        <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,8,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Log Files" Style="{StaticResource SectionHeader}" />
                    <Button Content="Refresh" Command="{Binding RefreshLogFilesCommand}"
                            Style="{StaticResource SecondaryButton}" Padding="8,3" FontSize="11"
                            HorizontalAlignment="Left" Margin="0,0,0,8" />
                </StackPanel>

                <ListBox ItemsSource="{Binding LogFiles}"
                         SelectedItem="{Binding SelectedLogFile}"
                         BorderThickness="0" Background="Transparent"
                         FontSize="11" />
            </DockPanel>
        </Border>

        <!-- Right: log content -->
        <Border Grid.Column="1" Style="{StaticResource CardPanel}">
            <TextBox Text="{Binding LogContent, Mode=OneWay}"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="11"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     Background="Transparent"
                     BorderThickness="0" />
        </Border>
    </Grid>
</UserControl>